# External git refs auto-pulled by `chezmoi apply` / `chezmoi update`.
# Refresh weekly (168h) unless --refresh-externals=always is passed.

[".local/share/ai-config"]
  type = "git-repo"
  url  = "https://github.com/akshaybapat6365/ai-config.git"
  refreshPeriod = "168h"
  [".local/share/ai-config".clone]
    args = ["--depth", "1"]
  [".local/share/ai-config".pull]
    args = ["--ff-only"]

# bjarneo/quickshell external removed 2026-05-21 — researched as a candidate
# Mission Control overview, but it's a widget pack (music notifier + top bar
# that REPLACES waybar) with no workspace-overview module. Replaced by AUR
# quickshell-overview-git (Shanu-Kumawat/quickshell-overview), package-
# managed via metapac/groups/misc.toml and overlaid with the user config at
# dot_config/quickshell/overview/config.json.

# Application bindings — direct exec, NO uwsm-app wrapping. uwsm-app pipes
# spawns to wayland-wm-app-daemon (started at session boot on ws1), making
# the daemon the PID-tree ancestor. Hyprland's initial_workspace_tracking
# then places the new window on ws1 every time. The `[]` dispatcher prefix
# workaround does not survive the daemon indirection (verified empirically).
# Direct exec makes the spawned app the immediate child of Hyprland's
# dispatcher; PID tracking resolves correctly to the active workspace.
bindd = SUPER, RETURN, Terminal, exec, ghostty --working-directory="$(omarchy-cmd-terminal-cwd)"
bindd = SUPER ALT, RETURN, Tmux, exec, ghostty --working-directory="$(omarchy-cmd-terminal-cwd)" -e bash -c "tmux attach || tmux new -s Work"
bindd = SUPER SHIFT, RETURN, Browser, exec, omarchy-launch-browser
bindd = SUPER SHIFT, F, File manager, exec, nautilus --new-window
bindd = SUPER ALT SHIFT, F, File manager (cwd), exec, nautilus --new-window "$(omarchy-cmd-terminal-cwd)"
bindd = SUPER SHIFT, B, Browser, exec, omarchy-launch-browser
bindd = SUPER SHIFT ALT, B, Browser (private), exec, omarchy-launch-browser --private
bindd = SUPER SHIFT, M, Spotify, exec, omarchy-launch-or-focus spotify
# Canonical dual-surface YT Music entrypoints.
# Super+M reopens the last-used stable surface. Alt forces the TUI.
# Ctrl forces the Electron app. Legacy MPD / cliamp / Y-chord launchers are
# retired from normal flow now that both surfaces share one launcher contract.
bindd = SUPER, M, Music (auto), exec, /home/akshaybapat/.local/bin/music-launch auto
bindd = SUPER ALT, M, Music (tui), exec, /home/akshaybapat/.local/bin/music-launch tui
bindd = SUPER CTRL, M, Music (electron), exec, /home/akshaybapat/.local/bin/music-launch electron
bindd = SUPER SHIFT ALT, M, Stop all music, exec, /home/akshaybapat/.local/bin/music-stop
bindd = SUPER SHIFT, N, Editor, exec, omarchy-launch-editor
bindd = SUPER SHIFT, D, Docker, exec, ghostty --working-directory="$(omarchy-cmd-terminal-cwd)" -e lazydocker
bindd = SUPER SHIFT, G, Signal, exec, omarchy-launch-or-focus ^signal$ "uwsm-app -- signal-desktop"
bindd = SUPER SHIFT, O, Obsidian, exec, omarchy-launch-or-focus ^obsidian$ "uwsm-app -- obsidian"
bindd = SUPER SHIFT, W, Typora, exec, uwsm-app -- typora --enable-wayland-ime
bindd = SUPER SHIFT, SLASH, Passwords, exec, uwsm-app -- 1password

# If your web app url contains #, type it as ## to prevent hyprland treating it as a comment
bindd = SUPER SHIFT, A, Codex, exec, ghostty --working-directory="$(omarchy-cmd-terminal-cwd)" -e codex
bindd = SUPER SHIFT ALT, A, Claude Code, exec, ghostty --working-directory="$(omarchy-cmd-terminal-cwd)" -e claude
bindd = SUPER SHIFT, C, Calendar, exec, omarchy-launch-webapp "https://app.hey.com/calendar/weeks/"
bindd = SUPER SHIFT, E, Email, exec, omarchy-launch-webapp "https://app.hey.com"
bindd = SUPER SHIFT, Y, YouTube, exec, omarchy-launch-webapp "https://youtube.com/"
bindd = SUPER SHIFT ALT, G, WhatsApp, exec, omarchy-launch-or-focus-webapp WhatsApp "https://web.whatsapp.com/"
bindd = SUPER SHIFT CTRL, G, Google Messages, exec, omarchy-launch-or-focus-webapp "Google Messages" "https://messages.google.com/web/conversations"
bindd = SUPER SHIFT, P, Google Photos, exec, omarchy-launch-or-focus-webapp "Google Photos" "https://photos.google.com/"
bindd = SUPER SHIFT, X, X, exec, omarchy-launch-webapp "https://x.com/"
bindd = SUPER SHIFT ALT, X, X Post, exec, omarchy-launch-webapp "https://x.com/compose/post"

# Add extra bindings
# bind = SUPER SHIFT, R, exec, alacritty -e ssh your-server

# ---- Smart Universal Copy/Paste (overrides Omarchy default) ----
# Omarchy's default Super+C/V uses one Hyprland sendshortcut path for every app.
# clipboard-smart keeps non-terminals on that universal path, while terminals use
# PRIMARY-based copy and terminal-native paste routing so AI TUIs and shells keep
# bracketed paste behavior without logging clipboard text.
unbind = SUPER, C
unbind = SUPER, V
bindd = SUPER, C, Universal copy (smart),  exec, /home/akshaybapat/.local/bin/clipboard-smart copy
bindd = SUPER, V, Universal paste (smart), exec, /home/akshaybapat/.local/bin/clipboard-smart paste

# Hyprexpo: Mission Control / workspace overview (toggle on/off)
# Disabled until Arch ships hyprland 0.54.3-5 rebuilt against current libs
# (May 4 2026 lib upgrade left the running 0.54.3-4 with a stale ABI hash —
# every fresh hyprexpo build fails with "[he] Version mismatch"). Once the
# next hyprland package update lands, run:
#   hyprpm update -f && hyprpm reload -n
# and uncomment this line.
# bindd = SUPER, grave, Workspace overview (hyprexpo), hyprexpo:expo, toggle

# Phase 11 — wallpaper picker GUI. Moved off Super+Alt+P (user reported
# something outside Hyprland's source chain grabs it — likely Chromium PIP /
# screen-record / input-method handler). Super+Alt+I = "Image picker"; verified
# free across user + Omarchy default configs.
bindd = SUPER ALT, I, Wallpaper picker (waypaper), exec, waypaper

# Variety — auto-rotates wallpapers from internet feeds (Wallhaven, Unsplash,
# Bing, Reddit, Flickr, etc.). When it changes a wallpaper, set_wallpaper script
# at ~/.config/variety/scripts/set_wallpaper updates the Omarchy symlink and
# aether-bg-watcher fires to re-theme the whole system.
bindd = SUPER ALT, V, Variety (wallpaper feeds), exec, variety

# Hidamari — video / YouTube wallpapers. GTK GUI. Layers as a GTK fullscreen
# window above swaybg; kill with Ctrl+C in launching shell OR `killall hidamari`.
# Autostart disabled by default — runs only when launched.
bindd = SUPER ALT, W, Hidamari (video wallpaper), exec, hidamari

# Skwd-wall — closest-to-Aether wallpaper picker. Quickshell QML UI with:
# Wallhaven browser, color sorting (13 hue/saturation groups), Matugen palette
# extraction, video + WE scene support, tag system (optional Ollama AI tagging),
# random/cycle modes. Requires skwd-daemon (systemctl --user enable already done).
bindd = SUPER ALT, S, Skwd-wall (premium wallpaper picker), exec, skwd wall toggle

# ---- AI workflow ----
# Floating scratchpads — one per AI tool, persistent across workspaces
bindd = SUPER, BACKSLASH, AI scratchpad (Claude Code), exec, omarchy-ai-scratchpad claude
bindd = SUPER ALT, BACKSLASH, AI scratchpad (Codex), exec, omarchy-ai-scratchpad codex
bindd = SUPER SHIFT, BACKSLASH, AI scratchpad (opencode), exec, omarchy-ai-scratchpad opencode
bindd = SUPER CTRL, BACKSLASH, AI scratchpad (Hermes), exec, omarchy-ai-scratchpad hermes
# /ask Spotlight: prompt → claude --print → notification + clipboard
bindd = SUPER, PERIOD, AI quick ask, exec, omarchy-ask
# Floating Claude usage monitor (live token / rate-limit dashboard)
bindd = SUPER CTRL, U, Claude usage monitor, exec, alacritty --class ai-monitor --title ai-monitor -e claude-monitor
# Cross-CLI cost dashboard (Claude + Codex via ccusage)
bindd = SUPER CTRL, Y, Token cost session view, exec, /home/akshaybapat/.local/bin/omarchy-ccusage-report
# Skill picker — fzf over awesome-claude-code
bindd = SUPER CTRL, SLASH, AI skill picker, exec, alacritty --class ai-skill-picker --title ai-skill-picker -e omarchy-skill-picker
# Toggle the cheatsheet overlay (frosted-glass card layered above the wallpaper)
bindd = SUPER SHIFT, K, Toggle keybinds overlay, exec, omarchy-cheatsheet

# ---- AI window rules (Hyprland 0.53+: boolean actions need explicit 'on') ----
windowrule = workspace special:ai silent,          match:class ai-claude
windowrule = workspace special:ai-codex silent,    match:class ai-codex
windowrule = workspace special:ai-opencode silent, match:class ai-opencode
windowrule = workspace special:ai-hermes silent,   match:class ai-hermes
windowrule = float on,        match:class ai-(claude|codex|opencode|hermes)
windowrule = size 80% 80%,    match:class ai-(claude|codex|opencode|hermes)
windowrule = center on,       match:class ai-(claude|codex|opencode|hermes)
windowrule = opacity 1.0 1.0, match:class ai-(claude|codex|opencode|hermes)

# Common floating apps centered (calculator, color picker, 1Password)
windowrule = float on,  match:class (org.gnome.Calculator|hyprpicker|1Password)
windowrule = center on, match:class (org.gnome.Calculator|hyprpicker|1Password)

# /ask Spotlight overlay
windowrule = float on,      match:class ask-overlay
windowrule = center on,     match:class ask-overlay
windowrule = size 900 80,   match:class ask-overlay
windowrule = pin on,        match:class ask-overlay

# Phase 11 — youtube-music Picture-in-Picture popout. Floats in top-right
# corner, pinned across workspaces, drag-resizable via normal Hyprland
# window-resize gesture / Mod+RightMouse.
windowrule = float on,            match:title YouTube Music — Picture in picture
windowrule = pin on,              match:title YouTube Music — Picture in picture
windowrule = size 480 270,        match:title YouTube Music — Picture in picture
windowrule = move 100%-500 80,    match:title YouTube Music — Picture in picture
windowrule = opacity 0.95 0.92,   match:title YouTube Music — Picture in picture

# Floating monitor / picker windows
windowrule = float on,     match:class ai-(monitor|ccusage|skill-picker)
windowrule = center on,    match:class ai-(monitor|ccusage|skill-picker)
windowrule = size 70% 70%, match:class ai-(monitor|ccusage|skill-picker)

# ---- Live desktop widget (pinned to desktop layer via hyprwinwrap) ----
windowrule = float on,           match:class omarchy-widget
windowrule = pin on,             match:class omarchy-widget
windowrule = move 80 60,         match:class omarchy-widget
windowrule = size 460 700,       match:class omarchy-widget
# noborder/noinitialfocus aren't valid action names in Hyprland 0.54.3
# (workspace_rules format) — drop them; widget already has decorations=None via alacritty

# Music surface helpers retained in user space, but the supported UI paths are
# now the shared Electron launcher and the managed ytm-player TUI surface.

# ---- Open dashboard overlay (Super+H) ----
bindd = SUPER, H, Toggle live dashboard, exec, omarchy-dashboard

# ---- Linear workspace nav keyboard (§ E 2026-05-05) ----
# r-1 / r+1 = relative, no auto-create. Clamps at ws 1 and ws 9 (Hyprland
# refuses to create new workspaces when workspace_swipe_create_new = false).
# Super+1..9 absolute jump is wired in Omarchy default tiling-v2.conf.
bindd = SUPER ALT, H, Workspace prev, workspace, r-1
bindd = SUPER ALT, L, Workspace next, workspace, r+1

# Cheatsheet overlay — full-screen, page-level transparency in HTML.
# Hyprland window opacity stays at 1.0 so text remains fully opaque;
# the wallpaper bleed-through is achieved via CSS rgba() bg in the page.
windowrule = float on,            match:title OmarchyCheatsheetOverlay
windowrule = pin on,              match:title OmarchyCheatsheetOverlay
windowrule = size 1920 1174,      match:title OmarchyCheatsheetOverlay
windowrule = move 0 26,           match:title OmarchyCheatsheetOverlay

# Overwrite existing bindings, like putting Omarchy Menu on Super + Space
# unbind = SUPER, SPACE
# bindd = SUPER, SPACE, Omarchy menu, exec, omarchy-menu

# Logitech MX Keys
# bind = SUPER SHIFT, S, exec, omarchy-cmd-screenshot          # Print Screen Button
# bind = SUPER, H, exec, voxtype record toggle                 # Dictation Button
# bind = SUPER, PERIOD, exec, omarchy-launch-walker -m symbols # Emoji Button

# ---- Aether (wallpaper → theme) launcher ----
# Env vars force WebKit2GTK GPU compositing — without this, gallery scroll is choppy
# because WebKit falls back to software rendering on some Wayland setups.
bindd = SUPER ALT, T, Aether (wallpaper-to-theme), exec, env WEBKIT_FORCE_COMPOSITING_MODE=1 WEBKIT_DISABLE_COMPOSITING_MODE=0 aether

# ---- Volume keys: target hardware Speaker sink directly ----
# EasyEffects Sink is the default (so apps get DSP), but @DEFAULT_AUDIO_SINK@
# would point to the virtual EE Sink — its volume just attenuates the input
# into the DSP chain, which produces a non-linear/limited response.
# Targeting the post-DSP Speaker sink with --device gives natural hardware
# volume control while the DSP chain still processes everything.
unbind = ,XF86AudioRaiseVolume
unbind = ,XF86AudioLowerVolume
unbind = ,XF86AudioMute
unbind = ALT,XF86AudioRaiseVolume
unbind = ALT,XF86AudioLowerVolume
bindeld = ,XF86AudioRaiseVolume, Volume up, exec, omarchy-swayosd-client --device alsa_output.pci-0000_00_1f.3-platform-sof_sdw.HiFi__Speaker__sink --output-volume raise
bindeld = ,XF86AudioLowerVolume, Volume down, exec, omarchy-swayosd-client --device alsa_output.pci-0000_00_1f.3-platform-sof_sdw.HiFi__Speaker__sink --output-volume lower
bindeld = ,XF86AudioMute, Mute, exec, omarchy-swayosd-client --device alsa_output.pci-0000_00_1f.3-platform-sof_sdw.HiFi__Speaker__sink --output-volume mute-toggle
bindeld = ALT, XF86AudioRaiseVolume, Volume up precise, exec, omarchy-swayosd-client --device alsa_output.pci-0000_00_1f.3-platform-sof_sdw.HiFi__Speaker__sink --output-volume +1
bindeld = ALT, XF86AudioLowerVolume, Volume down precise, exec, omarchy-swayosd-client --device alsa_output.pci-0000_00_1f.3-platform-sof_sdw.HiFi__Speaker__sink --output-volume -1

# omarchy-spend ambient widget
# Note: SUPER ALT, S was bound to skwd wall toggle above; this rebinds it to the
# ambient spend widget. Restore skwd with: bind = SUPER ALT, S, exec, skwd wall toggle
windowrulev2 = float, class:^(org\.omarchy\.spend\.ambient)$
windowrulev2 = size 200 80, class:^(org\.omarchy\.spend\.ambient)$
windowrulev2 = move 100%-220 50, class:^(org\.omarchy\.spend\.ambient)$
windowrulev2 = noanim, class:^(org\.omarchy\.spend\.ambient)$
windowrulev2 = pin, class:^(org\.omarchy\.spend\.ambient)$
windowrulev2 = noborder, class:^(org\.omarchy\.spend\.ambient)$
unbind = SUPER ALT, S
bind = SUPER ALT, S, exec, omarchy-spend --ambient
